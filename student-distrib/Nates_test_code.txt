void switch_video(int terminal_num)
{
    if(current_terminal == terminal_num)
      return;
    switch(terminal_num){
      case 1:
          memcpy(OFFSET_VIDEO,&vid_mem_buf[terminal_num],OFFSET_4K);
          //TODO: Change mapping of video memory to corresponding buf
          memcpy(&vid_mem_buf[1], OFFSET_VIDEO, OFFSET_4K);
          //TODO: Change mapping of video memory from corresponding buf to actual video memory
          break;
      case 2:
          //repeat for 2
      case 3:
          //repeat for 3
      
    }
}
